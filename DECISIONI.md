# DECISIONI — REGOLO STP

> Le scelte prese e perché; le scelte aperte con le opzioni. Claude propone, decide chi segue
> il progetto. Una voce «aperta» che blocca una fase ferma la fase, non si aggira.

## Aperte

| # | Decisione | Opzioni (con costo) | Chi decide | Entro | Blocca |
|---|---|---|---|---|---|
| 1 | **Tono del sito: A «Lo studio» / B «Il cantiere» / mix** (prototipi in `opzioni/`) | A: esploso strutturale SVG (M) · B: percorso-domanda + prima/dopo (B+B) · mix: es. hero di A + percorso di B | cliente con FT | prima della fase 5 | fase 5 |
| 2 | Dominio: brasili.net / dominio REGOLO / entrambi con redirect | brasili.net è reputazione acquisita: se si cambia, redirect 301 pagina per pagina | cliente | prima della fase 6 | fase 6 |
| 3 | Continuità di marca con «Brasili» | quanto si eredita nel nome e nei testi | cliente | prima dei contenuti | fase 4 |
| 4 | Peso privato/pubblico nel fatturato | cambia l'ordine di servizi e progetti in evidenza | cliente | prima della fase 3 | fase 3 (ordine) |
| 5 | Colori e font definitivi; logo | valori provvisori in `CLAUDE.md`; se arriva un logo si rivedono | cliente | prima della fase 5 | — |
| 6 | Wow n. 2 (rilluminazione WebGL di una foto, E1) | dentro (A) / fuori | NB, su budget | prima della fase 5 | — |
| 7 | Bilingue IT/EN | solo con committenza estera reale | cliente | prima dei contenuti | — |
| 8 | Sezione «Note»/approfondimenti | solo se qualcuno in studio la alimenta | cliente | prima del lancio | — |
| 9 | Crawler di addestramento: consentiti / bloccati | studio tecnico, nessuna opera dell'ingegno da proteggere: default proposto **consentiti** | cliente | fase 6 | fase 6 |
| 10 | Analytics | **senza cookie** (Vercel Analytics o Plausible) → nessun banner. Alternativa GA4 → banner completo | NB | fase 7 | fase 7 |
| 11 | Pannello per il cliente | default **no**: progetti nel repo, aggiornamenti a preventivo. Sì solo se pubblicano progetti spesso | cliente | prima del lancio | — |
| 12 | 3D | dipende dai CAD/BIM di 2-3 progetti (mail agli asset n. 9). Oggi **fuori** | NB, dopo la mail | — | — |
| 13 | Quali comuni presidiare in SEO | si parte da dove hanno già lavorato | cliente + noi | dopo la lista lavori | fase 6 |
| 16 | **«Comfort, energia, acustica» non ha una risposta nel passo 1 del brief.** Come si collega? | Il passo 1 ha sei risposte fissate da CLAUDE.md § Il form, e nessuna è questo servizio: la sua CTA produceva `?intervento=energia-acustica`, che il brief scartava — precompilazione muta. Corretto oggi togliendo il parametro (nessuna URL che promette e non fa). Resta da scegliere: **A** — una settima risposta nel passo 1 «Comfort, energia, acustica» (costo B, ma sposta un capitolato e allunga la prima schermata del brief da 6 a 7 bottoni) · **B** — la CTA di quel servizio precompila «Ristrutturazione o ampliamento», che è come lo descrive chi chiama (costo 0, ma il brief dirà una cosa un po' diversa da quella su cui l'utente ha cliccato) · **C** — si lascia così: quel servizio manda al brief senza precompilare (costo 0, ed è lo stato attuale) | NB con FT | prima della fase 6 | niente. Oggi la CTA funziona, semplicemente non precompila |
| 15 | **Senza JavaScript, un rifiuto del server costa le cinque risposte del brief.** Va chiuso o si accetta? | **A** — si accetta come è adesso (costo 0): i `pattern` HTML aggiunti il 07/09 fanno fermare il browser sui casi noti (comune, nome, telefono), la sigla della provincia si normalizza, e resta solo il caso di un valore che il browser accetta e il server rifiuta. Chi ha JS non perde niente in nessun caso. · **B** — il route handler diventa una **Server Action** che ri-renderizza il form con i valori dentro e gli errori in linea, che è il modo giusto di fare un form senza JS (costo M: si riscrive `app/api/brief/route.ts` e il percorso di invio di `BriefForm`, e si ri-collauda tutta la fase 2) · **C** — al rifiuto si rimanda al form con le risposte nella query string (costo B, **ma sconsigliato**: nome, telefono e email finirebbero in una URL, quindi nella cronologia del browser e nei log del server — un problema di privacy peggiore del difetto che risolve) | NB | prima della fase 8 | il **collaudo** della fase 8, non le fasi 4-7 |
| 14 | **Come parte la mail del brief** (rivede la riga «form con Resend» del 03/09, sezione Prese) | A: Resend + dominio verificato — dipende dalla n. 2, codice già scritto · B: SMTP della casella dello studio — serve la password del titolare + ~30 righe (nodemailer) · C: SMTP di una casella dedicata `brief@` con app password — un posto Workspace + ~30 righe | NB con il titolare | prima della **fase 7** | fase 7 (`/privacy` deve nominare il responsabile del trattamento, e con B o C non c'è nessun terzo da nominare) |

## Prese

| Data | Decisione | Perché | Chi |
|---|---|---|---|
| 07/09/2026 | **n. 25 — L'accento passa da viola `#45327F` a blu inchiostro `#123C7A`** (emenda la n. 18, poche ore dopo) | La skill `refero-design`, installata da FT a metà fase, mette **indaco/viola come tell n. 1** del design generato: «ogni modello va di default su `#6366f1`, `#8b5cf6`, `#7c3aed` — è l'impronta digitale dell'IA». Misurato in HSL, `#45327F` sta a **255° di tonalità**, cioè in mezzo a quei tre (239° · 258° · 262°). La giustificazione che avevo — «è l'inchiostro del timbro» — reggeva come racconto ma non come difesa: non è il marchio a richiederlo, ed era l'unico posto in cui il sito spendeva colore. Spendere quel colore proprio sulla tinta più riconoscibile come «fatta da un modello», in una fase che esiste per non averla, era il difetto peggiore possibile. `#123C7A` sta a **216°**, cioè 39-46° lontano da tutti e tre, ha luminosità 27 % contro 58-67 %, e passa **meglio** su entrambi i fondi (10,75 e 6,05 contro 10,42 e 5,86). In più la giustificazione migliora: in Italia il tampone del timbro professionale è blu, non viola. Cambiano tre valori in `globals.css` e niente altro — è la prova che «i nomi dei token sono semantici» serve a qualcosa | NB, su segnalazione della skill |
| 07/09/2026 | **Fase 3 bis · n. 17 — La quota si disegna solo se passa tre condizioni**: due estremi su un oggetto reale in pagina · un numero **che il repo può contare** · una quantità **che il visitatore non conta a vista**. Tutte tre, o il filetto non si disegna | Alla fase 3 lo stesso apparato era già in pagina come occhiello — «SEZ. 01 — IL PUNTO DI PARTENZA», mono maiuscolo, centrato fra due filetti che non misuravano niente — cioè decorazione travestita da informazione, la definizione del cluster n. 5. Il modo in cui un apparato tecnico torna ornamentale è che **nessuno controlla il numero**. La terza condizione è quella che fa il lavoro: senza, passerebbero `4 voci`, `6 servizi`, `5 fasi`, `5 livelli` — numeri veri che il lettore conta a vista guardando lo schermo, sotto una quota che glieli dichiara. In tutto il sito ne restano **tre**: `5 ruoli`, `128 comuni · FM 40 · MC 55 · AP 33`, `passo n di 5`. Header, persone e barra CTA mobile non ne hanno, ed è il controllo: **se in pagina sono più di tre, la regola è stata violata**. `components/Quota.tsx` tipizza `numero: number`, così una quota senza numero non compila | NB |
| 07/09/2026 | **n. 18 — Un solo accento in tutto il sito, e sta solo sui segnaposto**: `timbro #123C7A` (blu inchiostro; **corretto in giornata da `#45327F`**, vedi n. 25). Muoiono `#2F4A42`, `#E4572E`, `#C7431D`, `accent-soft`, `accent-text`, `on-accent` | Le tre reference tier A **non hanno un accento cromatico** (misurato) e refero lo scrive come divieto; sbp dimostra che l'arancio è il *default di settore* dell'ingegneria, e `#E4572E` era già il difetto AA del 07/09 (bianco sopra 3,68:1, riparato con un terzo token — un accento che ha bisogno di due varianti per passare AA non è un accento). Ma **zero colore era misurabilmente sbagliato**: `.da-cliente` ha tre canali e due sono l'accento, e senza restano due su tre con il fondo a 1,1:1 dalla carta — il difetto già corretto il 07/09, su **62** segnaposto. Quindi l'accento resta, con un lavoro che nessun altro sito ha: **dichiarare ciò che manca**. È l'inchiostro del timbro professionale, e **si consuma**: quando i contenuti arrivano sparisce dalla pagina da sé. Non entra mai in header, bottoni, stati, fuoco, filetti, esploso, mappa | NB |
| 07/09/2026 | **n. 19 — Una famiglia per tema, zero monospace**: Archivo (A, con l'asse di larghezza) e Chivo (B). Escono Instrument Serif, Instrument Sans, Bricolage Grotesque e JetBrains Mono | La scelta è stata fatta **guardando**: 14 famiglie OFL impaginate con le parole vere del brief ai corpi veri (`kit/reference/_provini/PROVINI.md`). Newsreader e Source Serif escluse perché *sono* il cluster n. 1 (Instrument Serif con un altro nome); Geist perché è il carattere di Studio Foundry; Public Sans perché neutralità non è identità. L'asse di larghezza di Archivo ha una ragione funzionale e misurata: a 390 px il contrasto di scala estremo si ottiene **solo** se il display può stringersi. JetBrains Mono esce con un numero: Archivo e Chivo hanno `tnum` e cifre già di larghezza fissa, quindi le colonne si incolonnano con una riga di CSS. Bilancio: **40,6 + 27,0 KB, un file per rotta**, contro 60,8 (A) e 138,3 (B). I file si rigenerano con `scripts/genera-font.sh` (instancer + subset), come i comuni e il territorio: l'artefatto è nel repo, la ricetta accanto | NB |
| 07/09/2026 | **n. 20 — Le due opzioni prendono i due rami del bivio delle tier A**: A spende l'audacia sulla **scala** (7,3× con un salto fuori scala, il contrasto misurato di Storey), B sulla **densità e sulla tabella** (2,6×, quello misurato di AS) | `kit/reference/SCHEDA.md` § 2 dimostra che le tre tier A non condividono un'estetica ma tre meccanismi, e che si dividono su una cosa sola: o l'audacia sta nella scala (Kononenko 10,9× · Storey 7,3×) o sta nella densità (AS 2,6×). Non si possono avere entrambe. Le due home hanno bisogni diversi e due wow diversi — A finisce sull'esploso, che è un disegno; B sul prima/dopo, che è fotografia — quindi il bivio si divide invece di sceglierne un ramo solo. **Non è una differenza di valore: è una differenza di meccanismo**, e si vede scorrendo. In B il valore 48 px, che la lista di calibrazione nomina («un solo peso di titolo a 48 px») e che non appartiene a nessuna scala 1,125, non compare da nessuna parte | NB |
| 07/09/2026 | **n. 21 — Il fondo di B è a luminanza media** (`getto #C0C3C1`, L 0,541) e non cambia mai dall'header al footer; A è bianco puro con due tagli netti verso il nero | La leva è la **luminanza**, non la temperatura: refero mette a token un canvas a L 0,541 come colore più usato del suo sistema, ed è la sola prova nella cartella che un fondo medio regge una pagina intera. Prendo esattamente quella luminanza (11,82:1 contro 11,83) e rifiuto la temperatura, perché `#C4C3B6` ha il blu 14 punti sotto il rosso, cioè è la carta calda del cluster 1. **Un fondo medio non si eredita**: il testo secondario di refero (`#595855`) su questo fondo dà 4,00:1 e non passa AA, quindi ogni coppia è stata ricalcolata. Conseguenza da conoscere: in B **non esiste un terzo livello di grigio** — sopra `#4A4A4A` si cade sotto soglia — e la gerarchia si fa col peso e col posto, mai con l'`opacity` | NB |
| 07/09/2026 | **n. 22 — Zero bottoni nell'header**, in entrambi i temi: sopra la piega la CTA è una sola, quella della hero | Kononenko misurato: «in tutto il sito non esiste un bottone», e il menu è una frase con le virgole. Nei due prototipi la pastiglia «Raccontaci il progetto →» stava in alto a destra **e** nella hero, cioè due volte nella stessa schermata: la prima diceva al visitatore che il sito ha qualcosa da vendergli prima di avergli detto chi è. `CLAUDE.md` § Homepage, blocco 1 chiede «una sola CTA». Sotto i 896 px la scomparsa `<details>` la contiene, perché lì la hero è più lontana. Sotto il marchio c'è la sede in chiaro: su un sito che è il *secondo* contatto, un indirizzo nella prima riga vale più di un pittogramma | NB |
| 07/09/2026 | **n. 23 — Il display di A è 132 px e non 152**, che era il valore del piano | Misurato in pagina, non stimato: a 152 le quattro righe del payoff misuravano 511 px e, sommate agli 89 px dell'header e ai 44 della barra della proposta, spingevano la CTA a 370 px **sotto** la piega. Il wireframe del piano metteva la CTA a y=880 senza contare i 133 px di cornice che la pagina ha davvero. 132 / 18 = **7,3×**, che è esattamente il contrasto di scala misurato su Storey: il numero non è un compromesso arbitrario, è un valore della cartella di prove. Nella stessa passata è caduto il grigio sulla seconda metà del payoff: tingerla la faceva leggere come disattivata, ed era la tell «una parola del titolo in un altro colore» con un vestito nuovo. La differenza è **larghezza e peso**, o non è | NB |
| 07/09/2026 | **n. 24 — L'asse verticale di B, su telefono, va al margine (8 px) invece che al 24,6 %** | Al 24,6 % (96 px a 390) **passava in mezzo al testo**: verificato in pagina, tagliava l'h1, il lead e le etichette della quota. Su 358 px di contenuto non c'è spazio per due zone, e indentare tutto oltre l'asse lascerebbe ~30 battute di misura. Al margine il gesto è lo stesso — le cose pendono da una linea — ruotato nello spazio che c'è. **Non è una decorazione spenta**: è la versione mobile progettata che `CLAUDE.md` § Regole, 5 chiede. Il difetto è stato trovato guardando lo screenshot, che è la ragione per cui la skill dice di farseli | NB |
| 03/09/2026 | Stack: Next.js App Router + TS, Tailwind, GSAP + ScrollTrigger, Lenis, Vercel, form con Resend | stack standard della libreria; `next/image` fa la differenza su un sito di fotografie | NB |
| 03/09/2026 | Il sito è il **secondo** contatto; azione primaria = brief qualificato a 5 passi | scheda nicchia studi tecnici | NB |
| 03/09/2026 | Impianto tier B (Storey, Studio Foundry), estetica tier A, mai l'impianto dei grandi studi | scheda nicchia | NB |
| 03/09/2026 | Esploso strutturale in SVG a livelli, non in 3D | il 3D entra solo con i CAD del cliente | NB |
| 07/09/2026 | La home si costruisce in **due varianti** (`/` = A, `/opzione-b` = B) fino alla fase 3, poi si tiene quella scelta | FT deve vendere con due opzioni; costo della seconda variante: solo l'ordine dei blocchi e i token | NB |
| 07/09/2026 | Next.js 16 + React 19, Tailwind **v4** (token in `@theme inline` dentro `app/globals.css`, non in `tailwind.config`) | Tailwind v4 non usa più un file di config JS: i token stanno nel CSS. `@theme inline` è la forma documentata quando il valore è una variabile che cambia col tema — senza `inline` i colori di A resterebbero congelati anche dentro B | NB |
| 07/09/2026 | Due **root layout** in `app/(a)` e `app/(b)`, uno per tema | è il modo documentato di avere due `<html>` diversi (e quindi due `data-theme`) nello stesso progetto. Costo: navigare fra `/` e `/opzione-b` ricarica la pagina, e il 404 globale ha bisogno di una rotta catch-all in `(a)`. Alla fase 5 resta un layout solo e la complicazione sparisce | NB |
| 07/09/2026 | Tema B: aggiunto `accent-strong #C7431D` per i fondi pieni, l'arancio `#E4572E` resta per il testo grande | bianco su `#E4572E` dà 3,68:1, sotto il minimo AA di 4,5:1: il CTA principale dell'opzione B non era leggibile a norma. L'arancio acceso resta dov'è grande (titoli), il fondo del bottone si scurisce di poco | NB |
| 07/09/2026 | Font della sola opzione B con `preload: false` | Turbopack mette i `@font-face` dei due temi in un unico chunk CSS e Next precarica tutto quello che ci trova: l'opzione A si scaricava 105 KB di Bricolage e JetBrains Mono senza usarli | NB |
| 07/09/2026 | `/opzione-b` è `noindex` | è una rotta di proposta, non una pagina del sito. Lighthouse SEO su quella rotta dà 60 per questo motivo: è voluto | NB |
| 07/09/2026 | Il brief è **un solo `<form>` con i cinque passi tutti nel DOM**; la divisione a passi arriva con l'idratazione | è il solo modo di far funzionare davvero il form senza JS senza mantenere due markup diversi. `montato` viene da `useSyncExternalStore` (snapshot server `false`, client `true`): il primo render lato client è identico all'HTML servito, niente disallineamento | NB |
| 07/09/2026 | Le risposte a bottone sono **`radio` nativi con il pallino visibile**, non `button aria-pressed` come nei prototipi; l'etichetta è vestita da card | un bottone senza JS non conserva la scelta; un radio sì, e da tastiera funziona già con le freccette. Il pallino **resta visibile** (confermato da FT): affidare lo stato al solo colore è fragile — i bordi stavano a 1,4:1 — e lo stesso componente deve leggersi in due palette | NB, confermata da FT |
| 07/09/2026 | Autocomplete dei comuni con **`<input list>` + `<datalist>` renderizzato lato server** | zero JavaScript, zero librerie, accessibile di serie, e su mobile usa la tendina del sistema. I 128 nomi stanno nell'HTML (12,4 KB gz di documento in tutto) e non nel bundle. L'alternativa — un combobox scritto a mano — costava ~200 righe di ARIA per fare peggio | NB |
| 07/09/2026 | Elenco dei comuni **generato** da `scripts/genera-comuni.mjs` sull'elenco ISTAT (FM 40 · MC 55 · AP 33 = 128), non scritto a mano | un nome di comune sbagliato su un sito locale è un difetto vero. Lo script verifica le numerosità attese e si ferma se l'elenco a monte cambia. Dato pubblico amministrativo: non è contenuto del cliente | NB |
| 07/09/2026 | Validazione in **un solo modulo isomorfo** (`lib/brief/validazione.ts`), usato dal client e dal route handler | una regola scritta due volte diverge. Senza JS il lavoro lo fa la validazione nativa del browser (`required`, `type="email"`, `maxlength`), che è HTML: `noValidate` si accende solo da montati | NB |
| 07/09/2026 | **Due mail separate, non `batch.send`** | il batch di Resend è atomico: una copia di cortesia rifiutata farebbe cadere anche la notifica allo studio, cioè si perderebbe il lead. Prima lo studio, poi la cortesia; se la cortesia non parte il brief è arrivato comunque e la pagina di esito lo dice | NB |
| 07/09/2026 | **Consenso: si registrano momento, testo, versione e pagina — non l'IP** | per provare *cosa* è stato accettato bastano quelli, e l'identità è nei dati di contatto stessi. L'IP resta in memoria pochi minuti per il solo rate limit e non finisce in nessuna mail: minimizzazione, e una cosa in meno da dichiarare in informativa. Il testo vive in `lib/brief/consenso.ts` con un controllo che fa fallire la build se quello mostrato e quello registrato divergono | NB |
| 07/09/2026 | Rate limit **in memoria**, 5 invii per IP ogni 15 minuti, con restituzione del credito sugli invii rifiutati | uno studio di cinque persone riceve qualche brief al giorno: un Redis è un servizio in più da mantenere per niente. Su Fluid Compute l'istanza si riusa, e quando è fredda il limite diventa più permissivo — mai più severo, quindi non chiude fuori nessuno. Il credito si restituisce perché chi non ha JS e sbaglia la mail cinque volte non va bloccato per un quarto d'ora | NB |
| 07/09/2026 | Trasporto su file dietro **`BRIEF_TRASPORTO=file`**, esplicito, non su `NODE_ENV` | `next start` gira con `NODE_ENV=production` anche in locale, ed è così che si guarda questo progetto: un gate implicito lì avrebbe disattivato la prova, o peggio si sarebbe acceso dove non doveva. Lo accendono `npm run dev` e `npm run start:prova`; su Vercel viene ignorato e l'invio si rifiuta | NB |
| 07/09/2026 | Con JS un invio fallito **resta sul form**, con le risposte al loro posto; le pagine di esito servono il percorso senza JS | far perdere cinque risposte per un errore di rete è il modo più sicuro di perdere il contatto. `/brief/non-inviato` esiste, è `noindex`, e mette il telefono dove si vede | NB |
| 07/09/2026 | `/contatti` è **dinamica per scelta**, dichiarata nella tabella delle misure di `STATO.md` così la fase 8 non la segnala come regressione: legge `?intervento=` sul server | il passo 1 precompilato deve funzionare anche senza JavaScript, e per farlo il valore va scelto lato server. Prezzo: nessuna cache CDN su una pagina che non ha immagini, non fa fetch e non è quella su cui si misura l'LCP (misurato: 640 ms). Le due home restano statiche | NB |
| 07/09/2026 | Tipo di immobile a **sette** voci: casa indipendente · appartamento · **condominio o parti comuni** · edificio intero · capannone o ufficio · terreno · altro | il catalogo blocchi F1 dice «ogni risposta è un bottone finché è possibile». «Condominio o parti comuni» l'ha aggiunta FT: per uno studio di ingegneria civile nelle Marche è una filiera intera (sisma, facciate, lavori condominiali) e il committente è un amministratore, non un privato — senza una voce propria il segnale finiva in «edificio intero» o in «altro» e si perdeva. Si cambia in `lib/brief/domande.ts` e cambia in tutti e tre i posti (markup, validazione, mail) | NB, corretta da FT |
| 07/09/2026 | Le mail **non promettono tempi di risposta** | CLAUDE.md § Obiettivo lo vieta, e quanto passa prima della telefonata lo decide lo studio. Se vogliono dichiarare un tempo, è una riga da aggiungere e sta in CONTENUTI-DA-CLIENTE.md | NB |
| 07/09/2026 | Il kit di progetto è stato spostato **dentro il repo**, in `kit/` | i prototipi e il capitolato servono a ogni fase: averli nel repo evita gli `--add-dir` e li versiona insieme al codice a cui si riferiscono. I riferimenti in `CLAUDE.md` sono stati aggiornati | FT |
| 07/09/2026 | **Il pannello dei cinque percorsi (hero B) cambia senza JavaScript**: `radio` nativi + `:has()`, cinque pannelli tutti nel DOM | il prototipo usava `button` + `aria-pressed` + `innerHTML`: senza JS il blocco che smista tutto il traffico non mostrava niente. Costo: ~2 KB di HTML per i cinque pannelli. Guadagno: funziona senza JS, funziona da tastiera di serie con le freccette, e zero byte nel bundle. Provato: cinque percorsi su cinque, un solo pannello visibile alla volta, quello giusto | NB |
| 07/09/2026 | Le sezioni condividono un guscio, `components/sezioni/Sezione.tsx` | respiro fra i blocchi, posizione dell'occhiello, larghezza dell'introduzione e nota di cantiere sono la stessa cosa in dodici blocchi: scritti dodici volte divergono al terzo. La «quota» (la linea di misura del disegno tecnico, motivo dell'opzione B) è lo stesso markup dell'occhiello di A: cambia un token, non un `if` sul tema | NB |
| 07/09/2026 | La geometria dell'esploso è **codice**, non un'immagine: `lib/esploso.ts` calcola l'isometria e il server la scrive nell'HTML | senza JS il disegno è completo, con `prefers-reduced-motion` resta fermo, e alla fase 5 si anima **solo** il `--dy` dei cinque `<g>` — cioè un `transform`. Un PNG non si separa per livelli e non si ritematizza. Peso: ~6 KB nell'HTML | NB |
| 07/09/2026 | Nell'esploso, le aperture dell'involucro hanno un **colore proprio** e stanno sulle due facce visibili | il prototipo le disegnava sul piano `x = 0`, che in questa isometria guarda dietro: quattro finestre su otto non si sono mai viste, e le altre avevano il colore della parete. Risultato: l'involucro leggeva come un blocco grigio invece che come un involucro. Corretto in `lib/esploso.ts`, con il perché nel commento | NB |
| 07/09/2026 | Ogni faccia dell'esploso è **≥ 3:1 sul fondo** e il colore non porta informazione da solo | la palette del prototipo aveva fondazioni e impianti a 1,5:1 sul fondo scuro: invisibili. Rifatta e verificata faccia per faccia con `scripts/contrasto.mjs`; i rapporti sono in coda a ogni token in `globals.css`. La legenda accanto ripete i cinque livelli in testo | NB |
| 07/09/2026 | Il blocco Territorio è il **perimetro vero** delle tre province in SVG (confini ISTAT via openpolis, semplificati Douglas-Peucker a ~100 m), generato da `scripts/genera-territorio.mjs` | niente mappa a tile: un tile server è un terzo che vede l'IP di chi visita, quindi una voce in più nell'informativa e un consent gate davanti alla mappa. Questo SVG pesa ~4 KB, non chiama nessuno e non ha cookie. E un perimetro vero non è un disegno che somiglia alle Marche | NB |
| 07/09/2026 | Sulla mappa **non si accende nessun comune**: solo la sede | la geometria di un confine non è un'affermazione, il punto di un comune sì — dice «qui abbiamo lavorato». Quali siano è la decisione n. 13, aperta. I 128 punti sono già in `lib/territorio.ts`: quando arriva la lista si accendono, e non serve toccare altro | NB |
| 07/09/2026 | Nel blocco Numeri la cella ha un **trattino**, e il segnaposto dichiarato è uno solo, nella nota | un `[[DA CLIENTE: mq progettati]]` impaginato a 44 px riempie la cella su tre righe: la striscia diventa illeggibile e la pagina sembra rotta invece che in attesa. Il collaudo della fase 8 trova comunque `[[DA CLIENTE` nella nota. È l'unica eccezione alla regola «un segnaposto per campo», e sta scritta in `lib/numeri.ts` | NB |
| 07/09/2026 | Il cursore del prima/dopo **compare solo dopo l'idratazione** | il `clip-path` lo muove JavaScript: senza JS un `input[type=range]` in pagina sarebbe un controllo afferrabile che non fa niente, che è peggio di nessun controllo. Senza JS si vedono le due metà tagliate al 50 % con entrambe le etichette leggibili. È la stessa regola del brief: quello che la piattaforma non sa fare da sola non si finge | NB |
| 07/09/2026 | La query string dello smistamento sta sul **secondo** salto: pastiglia → `/servizi/<slug>`, e la CTA del servizio → `/contatti?intervento=…#brief` | CLAUDE.md § Homepage, blocco 2 chiede «porta al servizio **e** precompila il passo 1 via query string». Metterla sul primo salto vorrebbe dire una `?intervento=` su `/servizi/<slug>` che non fa niente — o far leggere la query a una pagina che quel dato ce l'ha già, rinunciando alla resa statica di sei rotte. L'esito per chi compila è identico: provato senza JavaScript, home → servizio → brief con il passo 1 già scelto | NB |
| 07/09/2026 | Una `BarraProposta` in cima alle due home, con il link all'altra opzione | serve a FT in call: da una sola anteprima si mostrano entrambe le proposte, e la barra dichiara prima di ogni altra cosa che i contenuti sono indicativi. **Sparisce alla fase 5** con la rotta non scelta | NB |
| 07/09/2026 | Nell'opzione A **non ci sono** i blocchi Numeri e Testimonianze, in B non c'è il Territorio | è l'ordine di CLAUDE.md § Due opzioni, che è più specifico dei 13 blocchi di § Homepage e coincide con i due prototipi. Le testimonianze non stanno in nessuna delle due: sono `[[DA CLIENTE]]` al 100 % e i prototipi le hanno lasciate fuori. Non è una dimenticanza, ed è scritto in `STATO.md` | NB |
| 07/09/2026 | Il CSS delle sezioni sta in un file a sé, `app/css/sezioni.css`, importato da `globals.css` | `globals.css` era già 800 righe di token: il ritmo delle sezioni è un'altra cosa e si guarda insieme. Verificato che Tailwind v4 processi l'`@import` locale (`@layer components` e `@utility` funzionano). I token restano in `globals.css`, dove stanno gli altri | NB |
| 07/09/2026 | Nell'opzione B i progetti in evidenza sono **due**, non «3-4» come dice CLAUDE.md § Homepage, blocco 4 | in B ogni scheda porta la tabella dei dati duri (luogo, anno, superficie, ruolo, committente): due schede piene stanno su una riga e si leggono, tre schiacciano la tabella. È anche quello che fa il prototipo B approvato (`kit/opzioni/REGOLO_Opzione_B.html`). In A restano tre, come da capitolato | NB |
| 07/09/2026 | Le sezioni della fase 3 hanno transizioni su `background-color`, `border-color` e `color`, mentre CLAUDE.md § Regole, 7 dice «animare solo `transform` e `opacity`» | sono cambi di **stato** (hover, `:checked`, focus) da 0,15 s su proprietà di solo *paint*, non animazioni allo scroll: non fanno layout e `prefers-reduced-motion` le azzera (verificato: scendono a 0,00001 s). La regola 7 riguarda le animazioni continue, che è dove `width`/`top` costano frame. Toglierle vorrebbe dire un form e cinque percorsi senza nessun riscontro al passaggio del mouse. Deviazione dichiarata, non silenziosa | NB |
| 07/09/2026 | `placeholder-media` **non** è un `@utility` di Tailwind ma una classe in `@layer components`; l'etichetta del segnaposto pure | `@utility` compila in `@layer utilities`, che nella cascata viene dopo `@layer components`: ogni regola di blocco che provava a riposizionare un segnaposto perdeva **in silenzio**. Costo pagato prima di accorgersene: il blocco prima/dopo servito completamente vuoto (due rettangoli alti 2 px) e la targhetta della hero larga come tutta la foto. La nota in testa a `app/css/sezioni.css` spiega la regola per chi scriverà la prossima | NB |
| 07/09/2026 | I campi di testo del brief portano un `pattern` HTML | serve **solo** a chi non ha JavaScript, ed è la differenza fra un errore segnalato sul campo con le risposte al loro posto e un 303 che le porta via tutte e cinque. I `pattern` sono più permissivi della validazione del server, che resta l'ultima parola. Trappola trovata: i browser compilano `pattern` con il flag `v`, che dentro una classe di caratteri vuole `( ) / -` protetti — non protetti il pattern è invalido e viene **ignorato in silenzio**, cioè il campo sembra validato e non lo è | NB |
| 07/09/2026 | Il comune si normalizza prima di validarlo: via la sigla della provincia in coda, apostrofi tipografici uniformati | il `<datalist>` mostra «Fermo (FM)» e chi scrive quello che legge scriveva una stringa che il server rifiutava — sul campo che quasi tutti compilano per primo, e senza JS costava tutte le risposte. Verificato su tutti e 128 i comuni, in forma semplice e in forma «Nome (XX)»: nessun nome vero viene alterato | NB |
| 07/09/2026 | **Niente pittogrammi per i sei servizi** (il prompt della fase 4 li dava come possibilità) | la direzione visiva è tipografica: serif display grande, molto bianco, angoli vivi, niente ombre né gradienti (CLAUDE.md § Direzione visiva). Sei icone davanti a sei titoli che sono già degli esiti — «mettere in sicurezza la struttura» — aggiungono rumore e non informazione, e sono anche la cosa che invecchia prima. Se il cliente li chiede si disegnano in SVG, non si generano | NB |
| 07/09/2026 | Le FAQ nascono con `validato: false` e **non entrano nello schema `FAQPage`** finché non sono confermate | `kit/REGOLO_SEO-GEO-LEGAL.md` dice che i testi delle risposte vanno scritti con lo studio. Emettere `FAQPage` su una risposta non confermata vorrebbe dire farla comparire in un risultato di ricerca come se fosse dello studio. In pagina si vedono, marcate «proposta»: è lì che servono a chi legge e a chi le deve validare | NB |
| 07/09/2026 | La scheda progetto di esempio ha `esempio: true`, che la rende `noindex`, la tiene fuori dall'indice e le mette un cartello in cima | una scheda finta indistinguibile da una vera è la cosa peggiore che possa succedere in questa fase. L'interruttore è nel frontmatter, quindi la scheda vera nasce togliendo una riga e non ricordandosi di cancellare un file | NB |
| 07/09/2026 | Anno e superficie delle schede progetto restano **stringhe**, non numeri | finché sono segnaposto non sono numeri, e tipizzarli come numeri obbligherebbe a scriverci uno zero — cioè a inventare un dato. Alla fase in cui arrivano i progetti veri si valuta se stringere il tipo | NB |
